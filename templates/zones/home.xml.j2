#jinja2: keep_trailing_newline: True
<?xml version="1.0" encoding="utf-8"?>
<zone>
  <short>Home</short>
  <description>For use in home areas. You mostly trust the other computers on networks to not harm your computer. Only selected incoming connections are accepted.</description>
{% for service in firewalld_zone_home_services %}
  <service name="{{ service }}"/>
{% endfor %}
{% for interface in firewalld_zone_home_interfaces %}
  <interface name="{{ interface }}"/>
{% endfor %}
{% for port in firewalld_zone_home_ports %}
  <port port="{{ port.number }}" protocol="{{ port.protocol }}"/>
{% endfor %}
  <forward/>
</zone>
